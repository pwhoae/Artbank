---
title: "Godot 最强映射函数 remap() 保姆级教程#Godot教程 #Godot4 #remap函数 #游戏开发 #游戏编程 #GDScript #经验值条"
source: "https://www.bilibili.com/video/BV1KngJ6oEAH/?"
author:
  - "[[xcount]]"
published: 2026-08-15
created: 2026-08-30
description: "本期视频带你彻底搞懂 Godot 4.7 中的 remap() 函数，它能把任意范围的值按比例映射到新范围，再也不用自己写除法乘法的公式了！✨ 你会学到什么：remap() 的基本用法和返回值类型（浮点数/整数）超出范围或边界相等时的特殊处理（负无穷、NaN）结合 clamp() 限制映射结果与 lerp() / inverse_lerp() 的关系和区别三个实战案例：🔦 手电筒电"
tags:
  - "clippings"
---
<iframe width="560" height="315" src="https://player.bilibili.com/player.html?bvid=BV1KngJ6oEAH&amp;page=1&amp;high_quality=1&amp;danmaku=0" title="Bilibili video player" frameborder="0" allowfullscreen=""></iframe>

本期视频带你彻底搞懂 Godot 4.7 中的 remap() 函数，它能把任意范围的值按比例映射到新范围，再也不用自己写除法乘法的公式了！  
✨ 你会学到什么：  
remap() 的基本用法和返回值类型（浮点数/整数）  
超出范围或边界相等时的特殊处理（负无穷、NaN）  
结合 clamp() 限制映射结果  
与 lerp() / inverse\_lerp() 的关系和区别  
三个实战案例：  
🔦 手电筒电池电量 – 动态控制灯光强度  
📊 经验值进度条 – 无需调整最大值，自动适配升级所需经验  
🚶 脚步声调速 – 根据移动速度改变音调，让你的人物更真实  
🎯 无论你是刚学 GDScript 还是想优化代码，这个函数都能让你事半功倍。视频附带项目文件，描述区自取，欢迎三连支持～

## Transcript

**0:00** · 你可以免费下载视频中的艺术作品，请查看描述映射是一个函数，它接受一个范围内的值然后计算该值在该范围内的百分比进度并提供一个新值，该值在同一百分比位置但在新的范围内例如如果我们有一个从零到四的范围内的值一这相当于在过程中走了25% 意味着在新的零到一百二五的范围内从零到一百是二五这意味着此映射将等于二五

**0:28** · Remap 不需要接收浮点数作为值或范围你可以改用整数但映射结果始终为浮点数因此不会得到整数零而是会得到浮点数零点二五 Remap 基本上会将所有值视为浮点数处理这也意味着你可以混合使用浮点和整数而无需问题只需记住结果始终为浮点数除非将 Remap 放在 int 的括号内这会四舍五入并转换为整数

**0:57** · 移除小数部分当值超出范围时在映射过程中它会计算值的进度对于五在零到四的范围五相当于百分之一百二五在过程中百分之一百二五在零到一百的第二范围是百分之一百二五因此此映射等于一百二五这也意味着当值小于范围时会遵循相同逻辑处理负一

**1:24** · 在零到四的范围中等于负零点二五或负二五% 负二五%的零到一百将使映射等于负二五并且如果你想保持此映射值在第二范围的限制内可以添加 clamp 函数，将映射作为值并将最小允许值设为范围下限最大允许值设为范围上限或者你可以自定义这些最小值和最大值此外你无需在 clamp 或 remap 中使用变量

**1:53** · 可以直接输入数值我只是用变量使代码更易读然后有很多场景需要直接使用数值而非变量尽管始终建议使用变量为了使代码更具可读性当范围的最小值等于最大值时如果只有第一个范围的最小值和最大值相同则映射结果将等于负无穷当只有第二个范围的最小值和最大值相同则映射结果将等于第二个范围的值

**2:21** · 即使值超过第一个范围的最大值或低于最小值在这种情况下它将始终等于第二个范围的值如果两个范围的最小值都等于最大值则映射结果将等于n n或非数字最后关于映射函数需要注意的是，它本质上是循环和逆循环的结合和逆循环 Lp接受一个范围例如零到四然后使用权重作为目标值的百分比 0.5即百分之五十

**2:50** · 意味着亮度将等于在零到四的范围内处于两的位置逆循环则相反因此我们可以输入权重二因为二在零到四的范围内处于百分之五十的位置此逆循环将等于0.5或百分之五十通过这种方式我们可以结合两者模拟映射函数我们使用逆循环获取值在第一个范围内的百分比然后使用线性插值将百分比应用于第二个范围内的值

**3:20** · 为了澄清应始终使用映射函数而非循环与逆循环组合映射函数编写起来更简洁使用映射函数可确保正确放置范围或数值尤其是Godot在编写映射时会提示类型尽管lp和逆循环有各自用途但像映射函数存在时通常无需如此组合你的游戏可以是手电筒系统使用二维点光源作为手电筒的光当按下手电筒按钮且电量大于零时

**3:50** · 电量按delta值递减 delta是上一帧以来的时间量这基本实现使电量变量为五时持续五秒然后使光线可见并使用映射函数调整光的强度使用当前电量作为输入值从最大电量向零变化最大能量设为1.25 最小能量设为0.0 另一个例子是代码中的经验值条我们先增加经验值

**4:19** · 将当前经验值变量增加相应数值然后我们使用一个循环，会持续运行其代码直到当前经验值小于该等级所需经验值然后我们移除该等级所需的经验值提升等级然后按照之前提到的指数增长方式增加经验值需求，增幅为25% 这个循环将持续运行直到当前经验值小于所需经验值最后我们将进度条节点的值改为映射值该值在0到所需经验值范围内获取当前经验值并将此值映射到进度条的范围

**4:48** · 即从0到进度条的最大值通过使用映射函数在此情况下这实际上避免了需要手动调整进度条的最大值但无论新的经验值需求如何，都能简化代码另一个例子是根据移动速度调整行走音效的音调根据移动速度的快慢在此示例中每0.5秒当我移动时播放脚步音效在内部我们调整音效的音量根据移动速度进行调整我们使用映射函数，传入速度x轴值作为参数

**5:16** · 在0到最大速度范围内获取第二个范围即我们想要的音调值从最低0.8到最高1.3 此外因为玩家可能向左移动因此速度x轴值可能为负数使用abs函数将速度x轴值转为正数确保范围始终在0到速度之间现在你已基本掌握映射函数及其应用场景别忘了在描述中查看项目文件