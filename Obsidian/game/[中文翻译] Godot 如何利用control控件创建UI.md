---
title: "[中文翻译] Godot 如何利用control控件创建UI"
source: "https://www.bilibili.com/video/BV1nztd6wEQE/?"
author:
  - "[[乐煜城]]"
published: 2026-09-03
created: 2026-09-06
description: "[中文翻译] Godot 如何利用control控件创建UI, 视频播放量 459、弹幕量 0、点赞数 29、投硬币枚数 14、收藏人数 84、转发人数 1, 视频作者 乐煜城, 作者简介 ，相关视频：彻底解耦 用GDScript实现Vue的响应式系统 来控制Godot的节点属性，[中文语音] 精通Godot UI——使用Control节点构建界面，停止裸奔！丑戈多，给你的Godot套上底裤[深度闲谈+]，Godot UI案例，Godot 4.7 2D RPG 游戏开发全流程课 _ FSM 状态机 _ 背包装备 _ 技能对话 _ 商店任务，Godot4设计UI界面Figma，如何逆向一个Godot游戏？，Godot制作UI，锚点固定位置，Godot教程，Godot制作UI，主题把效率拉满"
tags:
  - "clippings"
---
<iframe width="560" height="315" src="https://player.bilibili.com/player.html?bvid=BV1nztd6wEQE&amp;page=1&amp;high_quality=1&amp;danmaku=0" title="Bilibili video player" frameborder="0" allowfullscreen=""></iframe>

## Transcript

**0:00** · 所以你发现控制节点真的很难上手，而且不管你怎么尝试， 都觉得怎么也搞不明白它们好吧，我来帮你改变这个状况在开始之前，这个视频并不是所有可用控制节点的完整列表， 也不是关于它们所有属性的讲解那不太现实这个视频将深入解释在Godot中制作用户界面所涉及的所有工具和概念

**0:27** · 我们将从概念入手，同时讨论常见的陷阱，以及拆解看似复杂的布局并将其实际融入到这些基本概念中的思考过程最后，我们将看一个实际例子，我会引导你完成设计一个简单而优雅的布局的过程，同时穿插一些超级实用的小技巧，这些技巧会让你的工作轻松很多请继续看下去让我们开始吧每个控制节点都有一些基本的变换属性，比如位置、旋转、尺寸和缩放

**0:57** · 位置在视觉上始终是节点的左上角这个蓝色框代表游戏窗口的分辨率这可以在项目设置中更改所以，如果我将面板的位置设置为项目分辨率的一半， 那么正方形的左上角将正好位于游戏屏幕的中心尺寸属性（不要与缩放混淆）定义了右下角相对于左上角有多少像素

**1:23** · 这意味着增加节点的尺寸只会让控件向右或向下生长缩放可以看作是尺寸属性的乘数节点的最终尺寸将是其尺寸乘以缩放值例如，如果节点的X轴尺寸是100像素，X轴缩放是2，那么最终尺寸将是200像素这里需要注意的一个重要点是，缩放依赖于枢轴偏移属性，即枢轴点相对于左上角的偏移量

**1:53** · 这个小小的加号图标显示了选中节点的枢轴点位置默认情况下，由于枢轴点在节点的位置，增加缩放会使节点像增加尺寸一样生长然而，如果我将枢轴偏移的X值设置为尺寸X值的一半， 你会看到增加X轴缩放会使面板向两个方向生长如果我将其设置为两个轴上都为尺寸的一半，它就会从中心向外生长请注意，枢轴偏移不会以任何方式影响尺寸属性

**2:21** · 旋转属性相当直观唯一需要注意的是，它和缩放一样，也遵循枢轴偏移节点在树中的顺序很重要在树中位置较低的节点在视觉上会叠放在上面所以当我将这个面板移到标签下方时，面板就会渲染在标签的上方锚点用于设计能在任何宽高比下正常工作的布局这些绿色的“钉子”就是控件的锚点

**2:49** · 锚点就是这些钉子围成的矩形的大小，分别是左、右、上、下右、上、下你可以在检查器中看到它们，如果你将布局模式设置为“锚点”，并且锚点预设为“自定义” 每个锚点都是一个0到1范围内的浮点数你可以把它想象成百分比值为0表示0%，0.5表示50%，1表示100%

**3:12** · 如果你也将窗口分辨率想象成从左上角开始的0到1的范围，那么理解锚点就相当简单了让我们在窗口的X轴中心位置画一条线这将是整个范围的50%，也就是0.5 现在，如果我将右锚点设置为0.5，你会注意到右边的绿色钉子出现在我画线的那条线的中心位置

**3:34** · 现在，如果我在项目设置中更改目标分辨率并重新加载项目，你会看到，右锚点同样是0.5，但现在它移动到了新分辨率的中间点所以，本质上，窗口分辨率中的像素数不再重要了我们只需要开始考虑相对于窗口边缘的屏幕空间百分比所有这些理论都很酷，但谁关心呢？ 你关心啊想象一下，你想把这个血条放在屏幕正中间

**4:03** · 你把游戏发给一个朋友，在他那边看起来没问题很好但然后你把它发给另一个在稍长分辨率下玩的朋友，结果，它不再居中了另一个例子，你有一些漂亮的UI向玩家显示游戏中非常重要的信息这两种情况的解决方案是什么？ 锚点在这里，如果我们将锚点设置为水平居中，无论分辨率多宽，我们的血条都会位于屏幕中央

**4:32** · 但是，如果游戏的垂直分辨率改变了，这仍然会出问题要解决这个问题，我们必须将锚点设置为屏幕的底部中间这将确保元素在垂直方向上锚定在底部，水平方向上居中在这里，我们可以将其锚定在屏幕的右中心当你使用锚点时，如果预设设置为“自定义”，你会看到一个叫“锚点偏移”的栏目这些是相对于各自锚点的像素偏移量我这里有一个面板

**5:02** · 让我重置它的大小和位置，然后把它的锚点设置成这样现在，你会注意到锚点偏移已经有了一些数值如果我重置每一个，你会看到面板占据了锚点内的整个空间为了进一步演示，我将把锚点设置为占据整个屏幕，并且也重置偏移量或者，我也可以使用“全矩形”锚点预设，这等同于执行了上述两个操作

**5:29** · 现在，如果我将预设切回“自定义”，并将左偏移设置为100像素，面板会缩小，现在距离左锚点100像素在这种情况下，由于锚点设置为整个窗口分辨率，面板会正确地响应距离窗口左边缘100像素的固定偏移，无论窗口大小如何要在所有边上看到效果，我将右偏移设置为负100 这个值必须是负数，因为我们要的是相对于右锚点向左偏移

**5:58** · 如果我设置为正100，它将位于锚点右侧100像素处顶部和底部偏移也是如此现在，无论窗口如何调整大小，面板的尺寸属性都会自动调整以适应我们设置的偏移量为什么这无关紧要？ 让我们回到那个血条的例子我们之前看到，当窗口大小改变时，血条的大小保持不变但是，假设你希望它根据窗口分辨率拉伸

**6:25** · 为此，我们需要将锚点设置为水平跨越整个窗口，然后适当地设置我们的偏移量看，好了当窗口变宽时，血条会拉伸需要注意的一点是，调整控件节点的大小会自动设置相应的偏移量如果你使用的是“自定义”预设，也就是，切换这个白色锚点按钮会使调整大小的操作也按比例移动锚点这意味着锚点偏移保持恒定

**6:55** · 最后，为了结束锚点的讨论，我们现在可以谈谈锚点预设了你会在下面这个长得像《任天堂明星大乱斗》的按钮下找到一些非常有用的预设的按钮每个按钮在被点击时都会做两件事首先，根据你选择的预设设置锚点，其次， 所有偏移量都被设置为零关于这一点有个小小的星标注释我一会儿会讲到为了看到实际效果，如果我将面板的锚点预设设置为“左上”，你会看到它被锚定在左上角你再也看不到面板了

**7:24** · 那是因为锚点偏移被设置为零， 这反过来影响了尺寸因为我们的锚点只聚焦在那一个点上，而且我们对该点的偏移量为零，我们最终得到一个尺寸为零的面板当我改变面板大小时，你会看到偏移量做出反应，当我改变偏移量时，你会看到尺寸做出反应尽管有时看起来偏移量并没有被设置为零，我一直没搞明白是什么导致了这种不一致，如果有人知道，请告诉我

**7:52** · 但无论如何，如果你从根本上理解了锚点和偏移量的工作原理，这只是一个轻微的不便你会立刻知道该怎么做才能得到你想要的结果一件非常重要的事情要注意的是，预设不是相对于窗口分辨率的，而是相对于父节点的矩形区域的如果我创建一个控件节点作为这个面板的子节点，然后摆弄预设，你会清楚地看到这种行为锚点是相对于面板放置的唯一的例外是根节点，它是相对于窗口分辨率的，因为它没有父节点

**8:23** · 如果你在听我唠叨了这么久之后对锚点仍然感到困惑，别担心，这很正常你可能已经注意到了，这不是一个循序渐进的教程我的目标是介绍相关概念，并提供实际例子说明它们为什么重要希望到目前为止，我至少做得还算过得去看完这个视频后，自己动手试一试当你做得足够多时，一切应该都会开始变得直观起来自定义最小尺寸

**8:53** · 在我看来，这个属性是理解如何在Godot中制作UI时需要掌握的最重要的属性实际上它非常容易理解它所做的就是限制控件的尺寸属性不能低于自身就这么简单回到同一个例子，如果我们的窗口变得太窄， 这个血条就会缩成一团我们可以通过给它一个自定义最小尺寸来限制这一点这样它的尺寸就不能再低于这个值了这个属性有一些非常规的用例

**9:23** · 但是脱离具体环境很难讨论这些所以如果遇到它们，我会尽力提出来哦，这个属性在使用容器时会变得非常重要所以现在让我们来讨论容器吧容器是一种能自动排列其子节点，并根据子节点的数量和大小来设置自身尺寸的节点这意味着容器的直接子节点不能自由变换这些属性由父容器根据其自身类型来管理

**9:52** · 这听起来可能非常受限，说实话，确实有点但学会如何应对这种限制将帮助你看到容器的真正威力但在我们学习如何做到这一点之前，让我们先快速了解一下几个重要的容器 HBoxContainer（水平盒子容器）将其所有子节点水平堆叠子节点在树中的顺序就是元素堆叠的顺序 VBoxContainer（垂直盒子容器）完全相同，但是垂直的 MarginContainer（边距容器）让你可以在其自身边界框和其内容之间提供一些空间

**10:22** · 最后，PanelContainer（面板容器）将其子节点封装起来，并在其边界框内绘制一个面板你90%的UI都只会涉及到使用这四种容器构建UI只需以逻辑方式嵌套这些容器理解每个容器的作用以及它们之间如何交互非常重要一个常见的陷阱是使用诸如PanelContainer这样的容器，然后奇怪为什么你不能轻易地排列它的子节点那是因为容器的子节点不能被变换

**10:49** · 而PanelContainer所做的只是有效地将其子节点限制在其边界内如果你想垂直排列子节点，你需要使用一个VBoxContainer作为PanelContainer的子节点，并将那些节点作为VBoxContainer的子节点如果你想在内容周围添加一些空间，那是MarginContainer的工作你需要在PanelContainer和VBoxContainer之间添加它为什么它必须放在那里呢？ 那是因为VBoxContainer会根据其子节点调整自身大小，我们希望MarginContainer在该结果空间周围添加边距

**11:19** · 而PanelContainer会根据MarginContainer的结果大小进行调整这一切都是关于以逻辑方式嵌套这些容器最常见的是VBoxContainer和HBoxContainer 你可以通过思考所有水平和垂直部分来分解几乎任何布局，用这些容器来表示这个布局只是使用了一个HBoxContainer将其分成两个主要部分左侧部分只是每个项目的一个VBoxContainer，列表中每个项目的内容只是另一个HBoxContainer

**11:46** · 右侧部分又是一个VBoxContainer，这些部分又是HBoxContainer 加入一些MarginContainer和PanelContainer，就完成了真的就这么简单继续，容器的子节点不能有锚点，所以你不能手动设置它们，也不能使用我们之前用的预设然而，当你选中一个容器的子节点时，你应该会看到这个新按钮出现在顶部这个按钮包含容器尺寸选项，用于指定子节点在父容器内的布局方式

**12:13** · 使用这些按钮等同于在检查器中设置容器尺寸选项假设我有一个PanelContainer，里面有一个子Panel 当我摆弄子Panel的水平对齐方式时，你会看到它做出相应的反应垂直对齐也是一样左边的选项都是“收缩”选项它们将子节点缩小到其最小尺寸，并对齐到相应的边，由于容器的子节点不能手动设置其尺寸属性， 默认情况下它们只会收缩到零右边的选项是“填充”选项

**12:43** · 它们使子节点在相应的轴上占据所有可用空间但如果我希望我的面板是左侧的一个部分，并且让它实际可见呢？ 我们使用自定义最小尺寸属性我可以将面板的自定义最小值设置为比如两个轴上都为100像素，然后将水平对齐设置为“收缩开始”，然后我可以将其垂直对齐设置为顶部、中间或居中，或者让它垂直填充，这样它总是拉伸以覆盖父容器的整个垂直尺寸

**13:12** · 请注意，这些对齐选项仅当节点是容器的子节点时才相关例如，如果我在这里查看根节点，它似乎也有对齐选项可用，但当我设置它们时，什么也不会改变这仅当此场景作为容器节点的子节点被实例化到另一个场景中时才会生效很多人遇到的麻烦是在容器内定位和调整TextureRect（纹理矩形）的大小如果你以前遇到过这个问题，希望现在开始会变得更容易理解我这里有一个PanelContainer

**13:41** · 我将把一个TextureRect添加为它的子节点我们看到对齐选项按预期工作，但似乎没有明显的方法来调整它的大小这里的技巧是将“扩展模式”设置为“忽略大小”，如果我们使用任何收缩选项，这将使我们的图像缩小到自身大小然后我们该怎么做呢？ 你猜对了，给它一个自定义最小值现在我们可以按照我们想要的方式对齐它了你还可以摆弄“拉伸模式”属性，以指定纹理在TextureRect边界内的行为方式

**14:08** · 继续，一个常见的错误是试图设置VBoxContainer子节点的垂直对齐或HBoxContainer子节点的水平对齐这似乎不会像你期望的那样工作以HBoxContainer为例，你不能设置其子节点的水平对齐，因为这已经由HBoxContainer控制了但你可以设置它的垂直对齐这种混淆经常发生，尤其是当有人想使用“填充”设置时为什么它没有填充到容器的填充大小呢？ 因为对于那个，还有另一个选项叫做“扩展”

**14:40** · 只有特定的容器允许其子节点在特定的轴上设置“扩展”标志在容器子节点上设置“扩展”标志会使子节点占据容器剩余的空间我将用VBoxContainer中的两个面板来演示这一点请注意，这些面板在Y轴上设置了自定义最小尺寸没有它，它们只会完全缩成一团所以我们有两个容器子节点，由于它们的最小尺寸而占据了一定的空间但是容器中的剩余空间没有被任何节点占据

**15:11** · 如果我希望白色面板占据剩余空间，我需要将其设置为“扩展” 如果我希望橙色面板占据该空间，我需要改为将其设置为“扩展” 这将推开白色面板，直到容器中的所有空余空间都被完全占据这里我可以使用自定义最小尺寸来调整白色面板的大小其余空间会自动被橙色面板占据如果我将两个面板都设置为“扩展”，你会看到它们会争夺相等数量的剩余空间

**15:38** · 这里我可以使用“拉伸比例”属性来指定哪个面板占据更多空间目前，两个面板的该值都设置为1 但如果我将白色面板的比例设置为2，它会比橙色面板占据两倍的空间你可以向此容器添加更多节点，我刚才描述的所有内容仍然以同样的方式工作请注意，如果“扩展”标志未被勾选，“拉伸比例”完全不起作用一个非常常见的困惑是，在这里切换这个选项并不等同于切换“扩展” 仅仅按下切换按钮什么也不做

**16:06** · 打开它，然后选择一个对齐设置，会将该对齐与“扩展”标志一起设置你可以仅使用自定义最小值，或者结合“扩展”，在VBoxContainer或HBoxContainer中拥有不同大小的元素 Godot中的主题是一种资源，它包含了每个控件节点类型应该是什么样子的信息每个可自定义的节点类型都包含自己的一套主题属性你可以在一个主题中为特定的节点类型设置这些属性的值，并将该主题应用于整个项目

**16:34** · 现在，每当你创建一个该类型的节点时，我们在主题中设置的值就会默认被使用我不会在这里更深入地讨论主题，但如果你想看一期专门讲解主题以及如何设置它们的视频，请告诉我不过，我刚刚描述的内容应该足以作为我们进入下一个话题的背景了主题覆盖在拥有主题属性的控件节点上（大多数控件节点都有），你会在检查器中看到一个叫做“主题覆盖”的部分

**17:02** · 在这里，你可以启用一个覆盖并设置一个新值请注意，与影响整个节点类型的主题不同，覆盖只应用于单个节点，而不是节点类型所以，如果我有两个标签，我覆盖了第一个标签的某个主题属性， 第二个标签仍然会使用主题中为标签节点类型定义的默认值这里要注意的另一件事是，如果该属性未被勾选启用，你在这里看到的值并不是当前正在使用的值覆盖只有在被打开时才会生效

**17:30** · 例如，VBoxContainer有一个“分离”主题属性，你可以用它来在每个子节点之间添加一些空间，默认情况下，尽管我们这里看到的是零，但看起来面板之间似乎有一些空间那是因为这个覆盖没有被应用，它只是使用了主题中为VBoxContainer定义的值当我们确实打开它时，我们看到子节点之间现在没有分离了这些主题属性对于自定义UI元素的外观非常有用

**17:55** · 我之前没有提到，设置MarginContainer的边距的方式就是使用它的主题属性 MarginContainer有属性来设置每一边的边距，不仅仅是容器，几乎所有控件节点，甚至像Label（标签）这样的东西，都有很多非常棒的主题属性，你可以覆盖并自定义成你喜欢的样子任何时候你想自定义任何控件节点的外观， 你应该首先查看该类的属性，当然， 但接下来要查看的地方就是它的“主题覆盖”部分

**18:26** · 一些控件节点拥有接受StyleBox（样式盒）的主题属性 StyleBox是定义控件节点区域样式的资源你可以通过检查节点的“主题覆盖”部分下是否有“样式”部分来了解它是否可以使用StyleBox 可以使用StyleBox的最简单的节点是Panel（面板） 你可以通过右键点击插槽并选择你想创建的StyleBox类型来创建一个StyleBox 你最常使用的类型是StyleBoxFlat（扁平样式盒） 点击资源以展开它

**18:53** · 你可以给它颜色、边框、圆角，还有一些其他属性你可以悬停查看它们的文档，它们都很直观我建议创建一个简单的Panel节点，如果你以前从未用过StyleBox，就去摆弄一下StyleBox的属性它们真的很容易使用，你可以用最少的工作量做出一些看起来很漂亮的布局一个非常常见的陷阱发生在复制带有StyleBox的节点时

**19:17** · 当我复制这个面板并更改其StyleBox中的颜色时，你会看到这改变了两个StyleBox的颜色那是因为StyleBox是资源，复制节点只是复制了对同一个资源的引用它不会复制资源对象本身要使这两个StyleBox独立，我需要右键单击其中一个并按下“设为独有”，这会使其成为一个完全独立的对象，现在我可以独立更改它们的属性了

**19:46** · 最后，我将带你完成制作这个简单布局的过程我将从创建一个PanelContainer并将其锚定到窗口中心开始我将给它一个漂亮的灰色StyleBoxFlat 同时，我将给StyleBox添加一个1像素的边框我还会通过设置这里的角半径属性来给它漂亮的圆角非常有用的提示你可以按Tab键将焦点移到下一个属性输入框，这样可以像我刚才那样轻松地输入一系列值让我们为场景添加一个背景，这样我们就不会看到这个难看的编辑器灰色了

**20:15** · 我将添加一个ColorRect（颜色矩形）节点作为兄弟节点，并将其锚点预设设置为“全矩形” 我需要将其在树中移到PanelContainer的上方，这样它就会在面板后面渲染我想把它的颜色设置成和面板一样的颜色，这样我就可以查看“最近颜色”部分来轻松选取那种颜色请注意，你可以在此处的“色板”部分保存你常用的颜色现在，我将在PanelContainer内创建一个Label（标签），并给它一些示例文本看起来我们需要垂直堆叠元素，所以我将在这里创建一个VBoxContainer，并把Label移进去

**20:43** · 我将Label设置为左对齐，这样当我们最终把PanelContainer放大时，Label节点会靠左对齐我将使用Ctrl+D复制Label，并将其用作副标题在“主题覆盖”下，我将覆盖其字体大小和颜色默认情况下字体看起来很模糊为了解决这个问题，我将进入项目设置，搜索MSDF，然后在“主题”选项卡中，打开这个选项我会确保保存我的场景，一旦我重新加载项目，哇，超级清晰的文字简直是天壤之别

**21:11** · 我真不敢相信这个选项默认不是开启的我们所有元素都贴在墙上开始让我感到厌烦了所以我将在PanelContainer内创建一个MarginContainer，并把所有内容都放进去在“主题覆盖”下，我可以给它所有边设置一些边距值看起来这些指标是水平排列的，所以我将在VBoxContainer内创建一个HBoxContainer，并创建一个Label和一个ProgressBar（进度条）节点，并将其设置为“扩展”，以便它占据剩余空间我需要再次减小指标Label的字体大小，所以我会借此机会向你展示另一种方法

**21:41** · 我将在Label上创建一个新的LabelSettings（标签设置）资源，并在那里设置字体大小我可以使用Ctrl+D复制这个节点，并将其在树中下移，将Label放在HBoxContainer的末尾这里使用LabelSettings资源的方法很有用，因为如果现在我决定要更改字体大小，更改任何一个Label都会同时更改两者这是因为它们共享同一个LabelSettings资源顺便说一句，在场景树焦点下，你可以按住Control键并使用箭头键在兄弟层级中上下移动选中的节点

**22:09** · 让我们正确地命名其中一些节点，因为如果所有东西都只是占位名称，UI树很容易变得混乱不堪在ProgressBar上，我将取消勾选“显示百分比”，这会使条缩小我将垂直对齐设置为居中，并给它一个自定义最小值来定义我想要的高度在选中数值字段时，你可以使用上下箭头键来递增或递减它按住Shift键可以增加10个单位，按住Ctrl键增加100个，按住Alt键增加0.1

**22:34** · 在我默认主题下这里有点难看清楚，但当我放大时，你会看到ProgressBar有一个深灰色区域这是ProgressBar的背景当我增加“值”属性时，会有一个覆盖在上面的另一个条来显示进度我可以通过为它们创建StyleBox覆盖来控制它们各自的外观我将给背景一个StyleBoxEmpty（空样式盒），这样我就只看到填充条了然后我给填充部分一个StyleBoxFlat 我将它的颜色设置为漂亮的蓝色，并给它一些圆角关闭抗锯齿可以让条看起来不那么像素化

**23:03** · 我可以使用一个VSeparator（垂直分隔符）节点，将它移到第一个Label下方，并将其设置为“扩展”，在第一个Label和ProgressBar之间添加一些空间还记得我之前将ProgressBar设置为“扩展”吗？ 所以现在Separator和ProgressBar正在争夺HBoxContainer中的剩余空间我可以在任何一个上设置“拉伸比例”来指定谁占据更多空间我将ProgressBar的“填充模式”设置为“开始”，以匹配参考图我将复制指标HBoxContainer来创建第二个，并设置相关属性由于文本长度不同，这些进度条无法正确对齐

**23:33** · 所以我会给右边的两个百分比Label在X轴上设置一个自定义最小值，并将它们的水平对齐设置为“右” 请注意，我这里设置的是Label的属性，而不是容器的对齐方式在VBoxContainer的末尾，我将创建一个Button（按钮），设置它的文本，设置它的字体大小覆盖，左对齐它，设置它的自定义最小值，给它一个StyleBoxFlat， 颜色，角半径，并关闭抗锯齿我还将在StyleBox属性中添加一些内容边距来给它一些内边距

**24:00** · 我注意到标题元素、指标以及每个部分作为整体，它们之间的空间量不同所以仅仅设置我们的VBoxContainer的“分离”值并不能真正解决问题，因为它们都会拥有相同的间距我们可以做的是为每个部分创建单独的VBoxContainer，并将它们放入主VBoxContainer中，这样我们就可以给它们各自的分离值所以我会为标题再创建一个VBoxContainer，并把它们放进去对于指标部分我也一样做现在我可以给它们各自设置自己的“分离”覆盖了

**24:28** · 对于这里的间距，我将创建一个HSeparator（水平分隔符）节点，并将其放在两个新的VBoxContainer之间然后，我将给它一个StyleBoxEmpty，设置它的“分离”，然后复制它来调整底部间距现在我想回去调整我的PanelContainer的大小，但当我点击这里时，它总是选中它上面的MarginContainer，这真的很烦人按住Alt键并右键点击会弹出一个列表，列出鼠标下的所有节点， 你可以在这里选择你想要的那个或者，你可以按下这里的锁图标来锁定选中的节点

**24:57** · 请注意，你仍然可以选中子节点，如果你根本不想选中任何子节点，也可以切换锁旁边的那个按钮我会锁定MarginContainer，这样我就可以安心地选中我的面板了我会在按住Alt键的同时调整它的边界，这样它就会从中心缩放如果我尝试从中间任何地方拖动来移动面板，它总是会选中那些元素我能做的是按住Alt键并从任何地方拖动来移动选中的节点，并将其定位到我想要的位置最后，我现在可以通过将PanelContainer的边框颜色设置为更亮的灰色来完成所有工作

**25:27** · 好了，完成了等等这个看起来好眼熟哦，是的如果你觉得这个视频有帮助，如果你能订阅的话我将非常感激制作这个视频花了很多功夫我有一些想法，后续可以更详细地讨论控件节点如果你对此感兴趣，或者有任何其他想法，请告诉我哦，我最近开始在Fiverr上提供Godot初学者指导服务了完全披露，我不是专家

**25:55** · 但如果你是编程、C#或Godot的初学者，并且学习遇到困难，我可以帮你起步链接在描述中要查看关于Godot的100个小技巧，请查看这两个视频哦，我向你保证，它们不是那种满是人尽皆知的凑数技巧的视频我很确定你会学到一些新东西总之，感谢观看再见