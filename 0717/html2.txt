很好，这份简历信息已经足够我们搭一个**第一版可运行的高质量作品集网站骨架**了。我会直接帮你做成一个**可以本地跑起来的 React + Vite 项目结构 + 核心代码**，并且已经按照你的定位：

> **AI-assist programmer｜暗色｜克制｜科技感｜非模板感**

来设计结构和风格。

---

# 🧠 一、从你的简历提炼的内容结构

我已经帮你整理好了你网站要展示的“内容数据模型”（后面代码直接用）：

### 👤 基本信息

* 名字：**HO PO WA**
* 定位：AI-assist Programmer
* 技能：LLM / AI Agent / Full-stack AI
* GitHub：github.com/pwhoae
* Email：[pwhoae@connect.ust.hk](mailto:pwhoae@connect.ust.hk) 

---

### 🚀 项目（精选用）

* UST Simulator（Godot Card Game）
* Real-Time Posture Recognition System
* USTLink（Campus Forum App）
* Search Engine Website 

---

### 🧠 优势（我们转成卡片）

* AI Agent Design
* LLM Application
* Full-stack AI Deployment
* Workflow Automation (n8n)
* Generative Media Pipeline 

---

# ⚙️ 二、项目初始化（你先做这个）

```bash
npm create vite@latest portfolio
cd portfolio
npm install
npm install react-router-dom
npm run dev
```

---

# 📁 三、目录结构（建议）

```
src/
├── components/
│   ├── Hero.jsx
│   ├── About.jsx
│   ├── Projects.jsx
│   ├── Strengths.jsx
│   ├── Contact.jsx
│   └── Navbar.jsx
├── data/
│   └── portfolio.js
├── App.jsx
├── main.jsx
└── styles.css
```

---

# 🧾 四、数据文件（核心）

## `src/data/portfolio.js`

```js
export const profile = {
  name: "HO PO WA",
  role: "AI-Assist Programmer",
  description:
    "Focused on building AI-powered systems, agents, and full-stack intelligent applications.",
  email: "pwhoae@connect.ust.hk",
  github: "https://github.com/pwhoae",
};

export const projects = [
  {
    title: "UST Simulator",
    desc: "Godot-based card game simulation",
    img: "/images/project1.jpg",
  },
  {
    title: "Posture Recognition",
    desc: "Real-time AI posture detection system",
    img: "/images/project2.jpg",
  },
  {
    title: "USTLink",
    desc: "Campus forum mobile app",
    img: "/images/project3.jpg",
  },
  {
    title: "Search Engine",
    desc: "Custom search engine website",
    img: "/images/project4.jpg",
  },
];

export const strengths = [
  "AI Agent Design",
  "LLM Applications",
  "Full-stack AI",
  "Workflow Automation",
  "Generative AI Systems",
];
```

---

# 🎬 五、Hero 首页（重点）

## `Hero.jsx`

（参考你给的 prompt.txt，我帮你简化成第一版可运行）

```jsx
import { useEffect, useState } from "react";
import "./hero.css";

const videos = [
  "https://...1.mp4",
  "https://...2.mp4",
  "https://...3.mp4",
  "https://...4.mp4",
];

export default function Hero() {
  const [index, setIndex] = useState(0);

  useEffect(() => {
    const timer = setInterval(() => {
      setIndex((i) => (i + 1) % videos.length);
    }, 6000);
    return () => clearInterval(timer);
  }, []);

  return (
    <section className="hero">
      {videos.map((v, i) => (
        <video
          key={i}
          src={v}
          autoPlay
          muted
          loop
          className={`bg-video ${i === index ? "active" : ""}`}
        />
      ))}

      <div className="overlay" />

      <div className="hero-content">
        <h1>HO PO WA</h1>
        <p>AI-Assist Programmer</p>

        <div className="buttons">
          <a href="#projects">View Projects</a>
          <a href="#contact" className="ghost">
            Contact
          </a>
        </div>
      </div>
    </section>
  );
}
```

---

## `hero.css`

```css
.hero {
  position: relative;
  height: 100vh;
  overflow: hidden;
}

.bg-video {
  position: absolute;
  width: 100%;
  height: 100%;
  object-fit: cover;
  opacity: 0;
  transition: opacity 1s ease;
}

.bg-video.active {
  opacity: 1;
}

.overlay {
  position: absolute;
  inset: 0;
  background: rgba(0, 0, 0, 0.55);
}

.hero-content {
  position: relative;
  z-index: 2;
  height: 100%;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  color: #fff;
}

.hero h1 {
  font-size: 64px;
  letter-spacing: 4px;
}

.buttons {
  margin-top: 20px;
}

.buttons a {
  margin: 0 10px;
  padding: 12px 24px;
  border: 1px solid #fff;
}

.buttons .ghost {
  opacity: 0.6;
}
```

---

# 👤 六、About 模块

```jsx
import { profile } from "../data/portfolio";

export default function About() {
  return (
    <section className="section">
      <div className="container">
        <h2>About Me</h2>
        <p>{profile.description}</p>

        <div className="info">
          <p>Email: {profile.email}</p>
          <p>GitHub: {profile.github}</p>
        </div>
      </div>
    </section>
  );
}
```

---

# 🧩 七、Projects 模块（大卡片）

```jsx
import { projects } from "../data/portfolio";

export default function Projects() {
  return (
    <section id="projects" className="section dark">
      <div className="container">
        <h2>Selected Projects</h2>

        <div className="grid">
          {projects.map((p, i) => (
            <div className="card" key={i}>
              <img src={p.img} />
              <div className="card-content">
                <h3>{p.title}</h3>
                <p>{p.desc}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
```

---

# 🧠 八、Strengths 模块

```jsx
import { strengths } from "../data/portfolio";

export default function Strengths() {
  return (
    <section className="section">
      <div className="container">
        <h2>Capabilities</h2>

        <div className="grid small">
          {strengths.map((s, i) => (
            <div className="card simple" key={i}>
              {s}
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
```

---

# 📩 九、Contact（整屏收尾）

```jsx
export default function Contact() {
  return (
    <section id="contact" className="contact">
      <h2>Let's Build Something</h2>
      <p>pwhoae@connect.ust.hk</p>
    </section>
  );
}
```

---

# 🧱 十、App.jsx

```jsx
import Hero from "./components/Hero";
import About from "./components/About";
import Projects from "./components/Projects";
import Strengths from "./components/Strengths";
import Contact from "./components/Contact";

function App() {
  return (
    <>
      <Hero />
      <About />
      <Projects />
      <Strengths />
      <Contact />
    </>
  );
}

export default App;
```

---

# 🎨 十一、基础全局样式（关键）

```css
body {
  margin: 0;
  background: #0a0a0a;
  color: #eaeaea;
  font-family: system-ui;
}

.container {
  max-width: 1700px;
  margin: auto;
  padding: 80px 40px;
}

.section {
  padding: 100px 0;
}

.dark {
  background: #111;
}

.grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 40px;
}

.card {
  background: #1a1a1a;
  border: 1px solid #2a2a2a;
}

.contact {
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  flex-direction: column;
}
```

---

# ✅ 当前版本你已经得到：

✔ 可运行 React + Vite 项目
✔ Hero 视频背景
✔ 暗色高级风格
✔ 项目卡片结构
✔ AI定位内容已经嵌入
✔ 版心控制 OK

---

# 🚀 下一步我可以帮你做（很关键）

你下一步可以直接说：

### 👉「帮我升级到设计师级作品集」

我可以继续帮你做：

1. ✅ **UI 直接拉到 Awwwards 水平（不是模板）**
2. ✅ 动效（scroll reveal / blur / parallax）
3. ✅ 项目页（单独 case study）
4. ✅ Three.js 科技感背景
5. ✅ 更像 “AI 工程师品牌站” 而不是学生作品集
6. ✅ 帮你优化成 **找工作/实习可用版本**

或者你可以：

👉 发参考网站 / Dribbble / 截图
👉 我帮你 **1:1 复刻 + 提升设计层级**

我们可以把这个站做到：
**能直接拿去投 OpenAI / AI startup 的 수준。**
